var express = require('express');
var router = express.Router();
var Joi = require('joi');
var common = require('./common');

router.post('/api/tree', function (req, res, next) {});

module.exports = router;